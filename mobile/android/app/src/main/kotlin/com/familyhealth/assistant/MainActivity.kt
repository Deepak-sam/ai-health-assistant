package com.familyhealth.assistant

import io.flutter.embedding.android.FlutterActivity

/**
 * Standard Flutter v2 (embedding v2) entry point activity. No native
 * platform channels are registered here — everything the app needs is
 * covered by community Flutter plugins declared in pubspec.yaml, each of
 * which self-registers via the Flutter plugin registrant.
 */
class MainActivity : FlutterActivity()
